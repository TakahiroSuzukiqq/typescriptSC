  CLI
$ ng new pf --style=scss --routing                      #to start newProject with scss, do routing
$ npm install @angular/cli --save-dev                   #to setup the version
$ npm install @angular/compiler-cli --save-dev          #to setup the version
$ ng serve                                              #to start server
$ ng generate component home                            #to create a new component
$ ng g c about                                          #to minimize coding about creating new components

$ rm -rf .git                                           #to remove the original git
$ git checkout -b "dev-branch"                          #to create development branch
$ git checkout master                                   #to switch back to the master branch
$ git pull origin master                                #to pull out the changes from the Github repo
$ npm install @angular/animations@latest --save         #to get the angular animations
$ ng generate service data                              #to generate the service file called "data", you can also do "$ ng g s data"

$ ng build
   OR     
$ ng build --prod                                       #lots of optimization, more smaller and faster by about 85%(e.g. 3.3MB -> 365KB)
$ npm install -g angular-cli-ghpages   
$ ng build --prod --base-href="https://TakahiroSuzukiqq.github.io/typescriptSC/"
$ angular-cli-ghpages                                   #to publish the page

$ ng g component crypto
$ ng g service crypto

$ ng g c http-test
$ ng g service http-test

$ ng generate component weather
$ ng generate service weather
$ npm install chart.js --sav
